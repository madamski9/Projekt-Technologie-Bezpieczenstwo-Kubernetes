apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: ssr-client-node-modules
  name: ssr-client-node-modules
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
